import React from 'react'
import './style.css'
import { useSelector } from 'react-redux'

function Favs(){

    const favs = useSelector((state) => state)

    return(
        <div>
            
        </div>
    )
}

export default Favs;